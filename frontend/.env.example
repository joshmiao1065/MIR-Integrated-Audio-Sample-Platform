# Only needed if NOT using the Vite dev proxy (e.g. deployed frontend pointing at remote API)
# Leave unset when running `npm run dev` — the proxy in vite.config.ts handles /api automatically
VITE_API_URL=http://localhost:8000
